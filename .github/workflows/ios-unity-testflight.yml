name: Unity iOS (manual) → TestFlight

on:
  push:
    branches: [ main ]
  workflow_dispatch:

env:
  BUNDLE_ID: ${{ secrets.BUNDLE_ID }}

jobs:
  unity-export-ios:
    name: Export Xcode project (Unity)
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4

      - uses: actions/cache@v4
        with:
          path: Library
          key: Library-${{ runner.os }}-iOS

      - name: Xcode version
        run: xcodebuild -version

      - name: Assert UNITY_LICENSE is present
        env:
          UNITY_LICENSE: ${{ secrets.UNITY_LICENSE }}
        run: |
          if [ -z "${UNITY_LICENSE}" ]; then
            echo "UNITY_LICENSE is EMPTY (secret не проброшен в джоб)"; exit 1
          fi
          echo "UNITY_LICENSE length: $(printf %s "$UNITY_LICENSE" | wc -c)"

      - name: Normalize UNITY_LICENSE newlines
        env:
          UNITY_LICENSE: ${{ secrets.UNITY_LICENSE }}
        run: |
          clean=$(printf "%s" "$UNITY_LICENSE" | tr -d '\r')
          {
            echo "UNITY_LICENSE<<EOF"
            echo "$clean"
            echo "EOF"
          } >> "$GITHUB_ENV"

      - name: Build (Unity → iOS)
        uses: game-ci/unity-builder@v4
        with:
          targetPlatform: iOS
        env:
          UNITY_LICENSE: ${{ secrets.UNITY_LICENSE }}
          UNITY_EMAIL: ${{ secrets.UNITY_EMAIL }}
          UNITY_PASSWORD: ${{ secrets.UNITY_PASSWORD }}

      - uses: actions/upload-artifact@v4
        with:
          name: xcode-ios
          path: build/iOS

  xcode-archive-upload:
    name: Sign, archive & upload to TestFlight
    needs: [unity-export-ios]
    runs-on: macos-14
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: xcode-ios
          path: build/iOS   # <-- фикс: ровно 'build/iOS'

      - name: Inspect downloaded artifact
        run: |
          set -euxo pipefail
          pwd
          ls -la
          ls -laR build || true
          test -d build/iOS || { echo "❌ build/iOS не найден. Проверь upload шага."; exit 66; }

      - name: Xcode version
        run: xcodebuild -version

      - name: Install xcpretty
        run: sudo gem install xcpretty -N

      - name: Import signing certificate (.p12)
        uses: apple-actions/import-codesign-certs@v3
        with:
          p12-file-base64: ${{ secrets.IOS_P12_BASE64 }}
          p12-password: ${{ secrets.IOS_P12_PASSWORD }}

      - name: Install provisioning profile
        run: |
          set -euo pipefail
          mkdir -p "$HOME/Library/MobileDevice/Provisioning Profiles"
          PROFILE_PATH="$HOME/Library/MobileDevice/Provisioning Profiles/appstore.mobileprovision"
          echo "${{ secrets.IOS_MOBILEPROVISION_BASE64 }}" | base64 --decode > "$PROFILE_PATH"
          if [ ! -s "$PROFILE_PATH" ]; then
            echo "❌ Provisioning profile was not written or is empty at: $PROFILE_PATH"
            ls -lah "$HOME/Library/MobileDevice/Provisioning Profiles" || true
            exit 2
          fi
          echo "✅ Wrote provisioning profile to: $PROFILE_PATH"

      - name: Diagnose signing setup
        run: |
          set -euo pipefail
          PROFILE_PATH="$HOME/Library/MobileDevice/Provisioning Profiles/appstore.mobileprovision"

          echo "— Env Bundle ID: ${BUNDLE_ID}"
          echo "🔐 Installed signing identities:"
          security find-identity -v -p codesigning || true

          echo "📂 Listing profiles dir:"
          ls -lah "$HOME/Library/MobileDevice/Provisioning Profiles" || true

          if [ ! -f "$PROFILE_PATH" ]; then
            echo "❌ Provisioning profile not found at $PROFILE_PATH"
            exit 2
          fi

          echo "📄 Dumping provisioning profile to profile.plist"
          security cms -D -i "$PROFILE_PATH" > profile.plist

          echo "— Profile Name:"; /usr/libexec/PlistBuddy -c 'Print :Name' profile.plist || true
          echo "— UUID:"; /usr/libexec/PlistBuddy -c 'Print :UUID' profile.plist || true
          echo "— TeamIdentifier:"; /usr/libexec/PlistBuddy -c 'Print :TeamIdentifier:0' profile.plist || true
          echo "— application-identifier:"; /usr/libexec/PlistBuddy -c 'Print :Entitlements:application-identifier' profile.plist || true
          echo "— team-identifier entitlement:"; /usr/libexec/PlistBuddy -c 'Print :Entitlements:com.apple.developer.team-identifier' profile.plist || true

      - name: Bump build number (CFBundleVersion)
        run: |
          cd build/iOS
          agvtool new-version -all $GITHUB_RUN_NUMBER || true

      - name: Archive (manual signing, verbose log)
        env:
          TEAM_ID: ${{ secrets.TEAM_ID }}
          PROFILE_NAME: ${{ secrets.IOS_PROFILE_NAME }}   # имя из Dev Portal
        run: |
          set -euo pipefail
          cd build/iOS

          PROJ_PATH=$(find . -maxdepth 2 -name "*.xcworkspace" -print -quit)
          if [ -n "$PROJ_PATH" ]; then
            PROJ=(-workspace "$PROJ_PATH")
          else
            PROJ_PATH=$(find . -maxdepth 2 -name "*.xcodeproj" -print -quit)
            if [ -z "$PROJ_PATH" ]; then
              echo "❌ Ни .xcworkspace, ни .xcodeproj не найдено в $(pwd)."; ls -la; exit 66
            fi
            PROJ=(-project "$PROJ_PATH")
          fi

          SCHEME="Unity-iPhone"
          echo "✅ Using project: $PROJ_PATH"
          echo "✅ Using scheme : $SCHEME"
          echo "✅ Using profile: ${PROFILE_NAME}"

          echo "🧰 Show build settings (top 200 lines)"
          xcodebuild "${PROJ[@]}" -scheme "$SCHEME" -configuration Release -showBuildSettings | head -200 || true

          set +e
          xcodebuild "${PROJ[@]}" \
            -scheme "$SCHEME" \
            -configuration Release \
            -archivePath out.xcarchive \
            -destination 'generic/platform=iOS' \
            CODE_SIGN_STYLE=Manual \
            DEVELOPMENT_TEAM="${TEAM_ID}" \
            PRODUCT_BUNDLE_IDENTIFIER="${BUNDLE_ID}" \
            CODE_SIGN_IDENTITY="Apple Distribution" \
            PROVISIONING_PROFILE_SPECIFIER="${PROFILE_NAME}" \
            ONLY_ACTIVE_ARCH=NO \
            BUILD_LIBRARY_FOR_DISTRIBUTION=YES \
            | tee xcodebuild-archive.log
          STATUS=${PIPESTATUS[0]}
          set -e
          echo "xcodebuild exit code: $STATUS"
          test $STATUS -eq 0

      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: xcodebuild-archive-log
          path: build/iOS/xcodebuild-archive.log

      - name: Sanity check archive (detect Generic Archive)
        run: |
          set -euo pipefail
          cd build/iOS
          /usr/bin/plutil -p out.xcarchive/Info.plist || true
          if ! /usr/bin/plutil -extract ApplicationProperties xml1 -o - out.xcarchive/Info.plist >/dev/null 2>&1; then
            echo "⚠️ Похоже, это Generic Archive (нет ApplicationProperties)."
            echo "Попробуем убрать DocC-пакеты рядом с .app…"
            find out.xcarchive/Products/Applications -maxdepth 1 -type d -name "*.docarchive" -print -exec rm -rf {} \; || true
          fi
          if ! /usr/bin/plutil -extract ApplicationProperties xml1 -o - out.xcarchive/Info.plist >/dev/null 2>&1; then
            echo "❌ Архив по-прежнему Generic. Частая причина — Skip Install = NO у не-app таргета или лишние продукты."; exit 70
          fi

      - name: Create ExportOptions.plist (App Store)
        env:
          PROFILE_NAME: ${{ secrets.IOS_PROFILE_NAME }}
        run: |
          set -euo pipefail
          cd build/iOS
          /usr/bin/python3 - <<'PY'
          import os, plistlib
          plist = {
            "method": "app-store",
            "destination": "export",
            "signingStyle": "manual",
            "provisioningProfiles": { os.environ["BUNDLE_ID"]: os.environ["PROFILE_NAME"] },
            "signingCertificate": "Apple Distribution",
            "stripSwiftSymbols": True,
            "uploadSymbols": True
          }
          with open("ExportOptions.plist","wb") as f:
              plistlib.dump(plist, f)
          PY
          /usr/bin/plutil -lint ExportOptions.plist
          /usr/bin/plutil -p ExportOptions.plist

      - name: Export IPA
        run: |
          set -euo pipefail
          cd build/iOS
          xcodebuild -exportArchive \
            -archivePath out.xcarchive \
            -exportPath out \
            -exportOptionsPlist ExportOptions.plist | xcpretty
          ls -lah out

      - name: Setup Ruby & fastlane
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.2'
          bundler-cache: true

      - name: Install fastlane
        run: gem install fastlane -N

      - name: Write asc_api_key.json
        run: |
          cat > asc_api_key.json <<JSON
          { "key_id":"${{ secrets.ASC_KEY_ID }}",
            "issuer_id":"${{ secrets.ASC_ISSUER_ID }}",
            "key":"${{ secrets.ASC_API_KEY_P8 }}" }
          JSON

      - name: Upload IPA to TestFlight
        env:
          APPLE_APP_ID: ${{ secrets.APPLE_APP_ID }}
          TESTFLIGHT_GROUPS: ${{ secrets.TESTFLIGHT_GROUPS }}
        run: |
          IPA=$(ls build/iOS/out/*.ipa | head -n1)
          if [ -z "${TESTFLIGHT_GROUPS}" ]; then
            fastlane pilot upload --api_key_path asc_api_key.json --apple_id "${APPLE_APP_ID}" --ipa "$IPA"
          else
            fastlane pilot upload --api_key_path asc_api_key.json --apple_id "${APPLE_APP_ID}" --ipa "$IPA" \
              --distribute_external true --groups "$TESTFLIGHT_GROUPS"
          fi

      - uses: actions/upload-artifact@v4
        with:
          name: ipa
          path: build/iOS/out/*.ipa
